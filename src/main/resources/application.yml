
springdoc:
  swagger-ui:
    path: /swagger/swagger-ui.html
  api-docs:
    path: /swagger/api-docs.html
jwt:
  secret: '3qeWtg$^KT8JH&Vdp2AvP(YFju5!%b)akcDG@R4ZhCxz+BUNwEXrmsS*MIf'

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitness-tracker?createDatabaseIfNotExist=true
    username: root
    password: ${MYSQL_PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect:

  main:
    allow-circular-references: true
server:
  port: 8080
keyId: 8d6c9933-8aa0-489d-b38f-7c2e41168d4a
rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem

